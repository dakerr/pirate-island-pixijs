import { designConfig } from './designConfig';

export const boardConfig = {
  /** The top of the screen. */
  screenTop: -designConfig.content.height,

  /** The line that determines the bottom of the falling object */
  floatLine: 145,
};
